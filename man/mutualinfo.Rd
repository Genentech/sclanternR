% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutualinfo.R
\name{mutualinfo}
\alias{mutualinfo}
\alias{mutualinfo,LocusNMF,NULL-method}
\alias{mutualinfo,LocusNMF,CellAlleleCounts-method}
\alias{mutualinfo,FiniteMixtureModel,NULL-method}
\alias{mutualinfo,FiniteMixtureModel,CellAlleleCounts-method}
\title{Compute per-locus mutual information for a fitted model}
\usage{
mutualinfo(object, x = NULL, ...)

\S4method{mutualinfo}{LocusNMF,NULL}(object, x = NULL, ...)

\S4method{mutualinfo}{LocusNMF,CellAlleleCounts}(object, x = NULL, ...)

\S4method{mutualinfo}{FiniteMixtureModel,NULL}(object, x = NULL, ...)

\S4method{mutualinfo}{FiniteMixtureModel,CellAlleleCounts}(object, x = NULL, ...)
}
\arguments{
\item{object}{A LocusModel (LocusNMF or FiniteMixtureModel).}

\item{x}{A CellAlleleCounts providing the count data, or NULL (the
default) to compute mutual information from the model's frequency
matrix weighted by topic proportions (pi for FMM, colSums(L) for
LocusNMF).}

\item{...}{Not used.}
}
\value{
Named numeric vector of per-locus mutual information scores.
}
\description{
Computes the scaled mutual information (G-test statistic) between
topics/clones and alleles at each locus. Can be called on a cell subset
by first subsetting both the model and data.
}
\examples{
\dontrun{
model <- fitLocusNMF(counts)
mi <- mutualinfo(model, counts)

# Without data, uses model frequencies weighted by topic proportions:
mi_model <- mutualinfo(model)

# On a cell subset:
cells <- c("cell1", "cell2")
mi_sub <- mutualinfo(model[, cells], counts[, cells])
}

}
