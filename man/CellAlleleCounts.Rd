% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CellAlleleCounts-class.R
\name{CellAlleleCounts}
\alias{CellAlleleCounts}
\title{Construct a CellAlleleCounts object}
\usage{
CellAlleleCounts(counts, locus_map = NULL, cell_data = NULL)
}
\arguments{
\item{counts}{A matrix (dense or sparse) of allele counts with features
(locus-allele pairs) as rows and cells as columns. Feature names should
follow the format \verb{chr\{chr\}_\{position\}_\{allele\}}.}

\item{locus_map}{A DataFrame with columns \code{locus}, \code{allele}, and
\code{feature_name}, one row per feature (row of \code{counts}). If NULL, it is
derived from \code{rownames(counts)} using the standard feature name format.}

\item{cell_data}{A DataFrame of cell metadata whose rownames are cell
identifiers matching \code{colnames(counts)}. If NULL, an empty DataFrame
is created from \code{colnames(counts)}.}
}
\value{
A \code{CellAlleleCounts} object.
}
\description{
Construct a CellAlleleCounts object
}
